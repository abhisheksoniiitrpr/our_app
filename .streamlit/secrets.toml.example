# Copy this file to .streamlit/secrets.toml for local dev,
# or paste the same keys into Streamlit Cloud → App settings → Secrets.
# Never commit the real secrets.toml — it's in .gitignore.

HIS_PASSWORD = "Abhishek"
HER_PASSWORD = "Hazel"
